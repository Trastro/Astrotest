---
export interface Props {
  title: string;
  subtitle: string;
  image: string;
}
const { title, subtitle, image } = Astro.props;
---

<section class="hero">
  <img src={image} alt="Hero image" />
  <h1>{title}</h1>
  <p>{subtitle}</p>
</section>